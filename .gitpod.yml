image:
  file: .gitpod.Dockerfile

tasks:
  - name: Install development dependencies
    init: |
      mamba env create -f dev/conda.yaml -p dev/env -y
      
  - name: Install library for development
    init: | 
      pip install -ve .

  - name: Activate dev env
    command: |
      source activate dev/env